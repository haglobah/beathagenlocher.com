---
import { formatContentDate } from './contentDate'

const { date } = Astro.props
---

<span class="font-mono text-cornflower-light">
  <time datetime={date.toISOString()}>
    {formatContentDate(date)}
  </time>
</span>
