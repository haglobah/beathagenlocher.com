---
import Il from './InnerLink.astro'

const { to } = Astro.props

import GitHubSlugger from 'github-slugger'

const slugger = new GitHubSlugger()
const slug = slugger.slug(to)

---
<Il href={ slug }>{ to }</Il>
