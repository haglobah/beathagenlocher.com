---
import AgoClient from './AgoClient'
import type { FormatStyleName } from 'javascript-time-ago'

interface Props {
  when: Date | number
  format?: FormatStyleName
}

const { when, format = 'mini-now' } = Astro.props
---

<AgoClient client:load when={when} format={format} />
