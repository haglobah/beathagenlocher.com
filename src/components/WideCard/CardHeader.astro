---

const { title, org } = Astro.props

import Org from './Org.astro'

---

<>
<span class="absolute f-left--16--24 f-right-0-0 f-top--16--24 f-bottom-0-0 hidden rounded lg:block"></span>
<span>{title}</span>
{org !== "" && (
  <Org org={org}/>
)}
</>
